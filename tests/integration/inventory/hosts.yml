---
all:
  children:
    ilo:
      hosts:
        ilo_server:
          ansible_host: "{{ lookup('env', 'ILO_HOST') }}"
          ilo_host: "{{ lookup('env', 'ILO_HOST') }}"
          ilo_username: "{{ lookup('env', 'ILO_USERNAME') }}"
          ilo_password: "{{ lookup('env', 'ILO_PASSWORD') }}"